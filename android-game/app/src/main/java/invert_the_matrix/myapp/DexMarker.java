package invert_the_matrix.myapp;

/**
 * Empty marker kept only so Play Console anti-tamper protection can process
 * the native-only app bundle. All game/runtime code remains in C++.
 */
public final class DexMarker {
    public static final int VERSION = 1;

    private DexMarker() {
    }
}
