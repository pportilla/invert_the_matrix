# Copy this file to signing/keystore.properties for local release builds.
# Never commit the real keystore.properties file or the keystore it points to.
storeFile=signing/upload-keystore.jks
storePassword=replace-with-keystore-password
keyAlias=upload
keyPassword=replace-with-key-password
