# Invert the Matrix Web App

The browser game is a static HTML/CSS app. Runtime JavaScript is generated from
TypeScript source:

```sh
npm install
npm run build
```

Source of truth:

- `src/app.ts`
- `styles.css`
- `index.html`

Generated browser output:

- `app.js`
- `app.js.map`
