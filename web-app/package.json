{
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "generate:campaign": "node ../scripts/generate-campaign-data.mjs"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
